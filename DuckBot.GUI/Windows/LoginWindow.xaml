<Window x:Class="DuckBot.GUI.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="250" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Username:" Grid.Row="0"/>
        <TextBox x:Name="UsernameBox" Grid.Row="1" Height="30" Margin="0,5,0,10"/>

        <TextBlock Text="Password:" Grid.Row="2"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Height="30" Margin="0,5,0,10"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Login" Width="100" Margin="0,0,10,0" Click="OnLogin"/>
            <Button Content="Cancel" Width="100" Click="OnCancel"/>
        </StackPanel>

        <TextBlock x:Name="StatusText" Grid.Row="5" Margin="0,10,0,0" 
                   Foreground="Red" TextWrapping="Wrap"/>
    </Grid>
</Window>
